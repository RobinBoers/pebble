defmodule PebbleWeb.DashboardHTML do
  @moduledoc  false
  use PebbleWeb, :html

  def dashboard(assigns) do
    ~H"""
    It's just a cruel existence&mdash;like it's no point hoping at all.
    """
  end
end
